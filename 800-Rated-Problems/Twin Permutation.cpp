#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        //input
        long long n;
        cin >> n;
        vector<long long> a(n);
        for(long long i = 0; i < n; i++){
            cin>> a[i];
        }
        
        
    }
}